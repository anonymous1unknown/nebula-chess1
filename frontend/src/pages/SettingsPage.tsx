export default function SettingsPage() {
  return (
    <div className="panel">
      <h2>Settings</h2>
      <p>Theme switching is in the header. Auth tokens are stored locally for this MVP build. Add server-managed sessions or secure cookie refresh flows for production hardening.</p>
    </div>
  );
}
